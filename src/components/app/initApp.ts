import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App = () => React.createElement('p', null, 'App');

ReactDOM.render(React.createElement(App), document.getElementById('appRoot'));
